void main() {
  //modificadores são outras formas que asvariaveis podem ser criadas
  //são como constantes pois podem impedir de serem modificdas
  // existe dois modificadores:

  //final : palavra reservada que após ser criada n pode mais ser alterada
  //se possivel sempre cria suas variaveis imuaveis
  final nomeCompleto = 'Tamires Moreno Galvão';
  //nomeCompleto = 'Tamiruz' //não vai permitir

  //const : essa variavel tem que esta com o status setado em compilação,  ou seja, para rodar dentro do servidor/compilador
  //no exemplo abaixo a variavel nomeCompleto só estara pronta quando o programa for executado, sendo assim o const n permite seu uso
  //compilação: quando o código esta sendo compilador para rodar
  //execução é o runtime
  // const nomeCompleto2 = nomeCompleto; //só pode ter valores ja conhecidos, ou seja, se outras constantes
  //RESUMO
  //Variaveis Finais:
  // Não podem ser alteras depois de inicializadas (imutaveis)
  //São definidas do programa em tempo de execução (Runtime)
  // utilize sem moderação

  //Variaveis cont
  //Não podem ser alteradas depois de inicializadas (imutaveis)
  //São definidas no programa em tempo de Compilação
  //não podem receber valores de outras variaveis a não ser const
  // utilize sem moderação
}
